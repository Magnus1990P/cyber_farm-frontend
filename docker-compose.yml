services:
  frontend:
    image: cff:latest
    build: .
    container_name: frontend
    restart: unless-stopped
    environment:
      - "API_HOST=backend"
      - "API_PORT=5000"
    ports:
      - 3000:3000