# CyberFarm Frontend

The frontend service for generating views and manual editing of objects.  
Written in typescript, based on next.js v15 and TailWind CSS.

## Instructions
1. Build image using `DockerFile` or `docker compose`  
    docker compose up -d
1. Upload to docker image registry
1. Run using `docker compose up -d`